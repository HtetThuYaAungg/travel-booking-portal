services:
  next-starter-kit-frontend-portal-development:
    build:
      context: ../../
      dockerfile: docker/Dockerfile
    image: next-starter-kit-frontend-portal-development:latest
    ports:
      - "3000:3000"
    env_file:
      - ../../.env