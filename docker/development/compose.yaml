services:
  hotels-booking-portal-frontend-development:
    build:
      context: ../../
      dockerfile: docker/Dockerfile
    image: hotels-booking-portal-frontend-development:latest
    ports:
      - "3000:3000"
    env_file:
      - ../../.env