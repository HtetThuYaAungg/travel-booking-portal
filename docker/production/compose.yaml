services:
  hotels-booking-portal-frontend-production:
    build:
      context: ../../
      dockerfile: docker/Dockerfile
    image: hotels-booking-portal-frontend-production:latest
    ports:
      - "3000:3000"
    env_file:
      - ../../.env